ADMIN_EMAIL=info@sadhanaarts.com
ADMIN_PASSWORD=sadhana123
SESSION_SECRET=change-me-in-production

# Neon Postgres (Vercel Storage tab)
DATABASE_URL=

# Vercel Blob for photo uploads
BLOB_READ_WRITE_TOKEN=

# Contact form email (Resend). Without this, FormSubmit is used and
# info@sadhanaarts.com must click the first confirmation email.
RESEND_API_KEY=
# Optional after you verify sadhanaarts.com in Resend:
# CONTACT_FROM_EMAIL="Sadhana Arts <info@sadhanaarts.com>"
