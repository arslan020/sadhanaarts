ADMIN_EMAIL=info@sadhanaarts.com
ADMIN_PASSWORD=sadhana123
SESSION_SECRET=change-me-in-production

# Optional — connect in Vercel (Storage tab) so the client can save content and photos
KV_REST_API_URL=
KV_REST_API_TOKEN=
BLOB_READ_WRITE_TOKEN=
